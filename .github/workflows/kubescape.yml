name: Kubescape scan
on: [push, pull_request]


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Kubescape Action
      uses: otomato-gh/kubescape-action@v0.3.1
      with:
        ksversion: "v2.0.158"
        path: "ApiGateway/blog.yaml"
